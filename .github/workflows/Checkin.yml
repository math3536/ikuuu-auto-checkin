name: Checkin

on:
  schedule:
    - cron: "0 22 * * *"   # 北京时间 06:00（UTC+8）= UTC 22:00
  workflow_dispatch:

concurrency:
  group: checkin
  cancel-in-progress: true

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install deps
        if: hashFiles('package.json') != ''
        run: |
          npm ci || npm install

      - name: Run
        env:
          URL: ${{ secrets.URL }}
          CONFIG: ${{ secrets.CONFIG }}
          TG_BOT_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TELEGRAM_TO }}
        run: |
          node main.js
